import styled from './node_modules/components:/Section/styled-components'
import tw from './node_modules/components:/Section/twin.macro'

const StyledSection = styled.section.attrs({
  className: `news`
})``

export default StyledSection